<fieldset class="threadFieldset">
  <legend>Thread</legend>
  <label>
    Title
    <input class="navi-input" type="text" name="title" required maxlength="200" value="<%= typeof title !== 'undefined' ? title : '' %>" />
  </label>
</fieldset>
